{{ 'section-hero-banner.css' | asset_url | stylesheet_tag }}

<div class="hero-banner" id="hero-banner-{{ section.id }}">
  <div class="hero-banner__background">
    {% if section.settings.video_url != blank %}
      <video 
        class="hero-banner__bg-video" 
        autoplay 
        loop 
        muted 
        playsinline 
        {% if section.settings.background_image != blank %}
        poster="{{ section.settings.background_image | image_url: width: 1920 }}"
        {% endif %}
      >
        <source src="{{ section.settings.video_url }}" type="video/mp4">
      </video>
    {% elsif section.settings.background_image != blank %}
      <img
        src="{{ section.settings.background_image | image_url: width: 1920 }}"
        alt="{{ section.settings.background_image.alt | default: section.settings.heading | escape }}"
        loading="eager"
        width="1920"
        height="1080"
        class="hero-banner__bg-image"
      >
    {% endif %}
    
    <div class="hero-banner__overlay" style="background-color: {{ section.settings.overlay_color }}; opacity: {{ section.settings.overlay_opacity | divided_by: 100.0 }};"></div>
  </div>

  <div class="hero-banner__container page-width">
    <div class="hero-banner__content">
      {% if section.settings.heading != blank %}
        <h1 class="hero-banner__heading" style="color: {{ section.settings.heading_color }};">
          {{ section.settings.heading }}
        </h1>
      {% endif %}

      {% if section.settings.subheading != blank %}
        <p class="hero-banner__subheading" style="color: {{ section.settings.text_color }};">
          {{ section.settings.subheading }}
        </p>
      {% endif %}

      <div class="hero-banner__buttons">
        {% if section.settings.button_label_1 != blank %}
          <a
            href="{{ section.settings.button_url_1 }}"
            class="hero-banner__btn hero-banner__btn--primary"
            style="background-color: {{ section.settings.button_bg_1 }}; color: {{ section.settings.button_text_1 }};"
          >
            {{ section.settings.button_label_1 }}
          </a>
        {% endif %}

        {% if section.settings.button_label_2 != blank %}
          <a
            href="{{ section.settings.button_url_2 }}"
            class="hero-banner__btn hero-banner__btn--secondary"
            style="background-color: {{ section.settings.button_bg_2 }}; color: {{ section.settings.button_text_2 }};"
          >
            {{ section.settings.button_label_2 }}
          </a>
        {% endif %}
      </div>
    </div>

    {% if section.settings.product_image != blank %}
      <div class="hero-banner__product">
        <img
          src="{{ section.settings.product_image | image_url: width: 800 }}"
          alt="{{ section.settings.product_image.alt | default: 'Product image' | escape }}"
          loading="eager"
          width="800"
          height="800"
          class="hero-banner__product-image"
        >
      </div>
    {% endif %}
  </div>
</div>

{% schema %}
{
  "name": "Hero Banner",
  "tag": "section",
  "class": "section-hero-banner",
  "settings": [
    {
      "type": "header",
      "content": "Background"
    },
    {
      "type": "url",
      "id": "video_url",
      "label": "Background Video URL (.mp4)",
      "info": "Paste a direct link to an MP4 file hosted in your Shopify Files."
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Fallback Background Image",
      "info": "Displays while the video loads or on devices where video autoplay is blocked."
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "Overlay color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay opacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "default": 0,
      "unit": "%"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "NEVER HAVE BAD BREATH"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading color",
      "default": "#222222"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "SmartMouth™ is the only mouthwash scientifically proven to eliminate & prevent bad breath for 24 hours with just 2 rinses a day."
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#555555"
    },
    {
      "type": "image_picker",
      "id": "product_image",
      "label": "Product image (Optional right side)"
    },
    {
      "type": "header",
      "content": "Button 1"
    },
    {
      "type": "text",
      "id": "button_label_1",
      "label": "Button label",
      "default": "New & Improved Pour"
    },
    {
      "type": "url",
      "id": "button_url_1",
      "label": "Button link"
    },
    {
      "type": "color",
      "id": "button_bg_1",
      "label": "Button background",
      "default": "#F07D00"
    },
    {
      "type": "color",
      "id": "button_text_1",
      "label": "Button text color",
      "default": "#ffffff"
    },
    {
      "type": "header",
      "content": "Button 2"
    },
    {
      "type": "text",
      "id": "button_label_2",
      "label": "Button label",
      "default": "View Products"
    },
    {
      "type": "url",
      "id": "button_url_2",
      "label": "Button link"
    },
    {
      "type": "color",
      "id": "button_bg_2",
      "label": "Button background",
      "default": "#0072CE"
    },
    {
      "type": "color",
      "id": "button_text_2",
      "label": "Button text color",
      "default": "#ffffff"
    }
  ],
  "presets": [
    {
      "name": "Hero Banner"
    }
  ]
}
{% endschema %}